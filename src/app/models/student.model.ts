import { Department } from './department.model';

export class Student{
    //department = new Department();

    // constructor(){}
    //constructor(){ }
    sId: number;
    name: string;
    departmentId: number;
    //department:Department;

    //c: CommonService= new CommonService();
//     getDepartmentForStudent(id: number): Department
//   {
//     this.c.getSingleEntity<Department>(id,"http://localhost:64159/api/department").subscribe(department => this.department = department);
//     return this.department;
//   }
    //departmentName : string = this.department.dep;
}